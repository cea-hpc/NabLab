### DO NOT EDIT THIS FILE - it is machine generated ###

cmake_minimum_required(VERSION 3.10)
cmake_policy(SET CMP0074 NEW)
set(CMAKE_VERBOSE_MAKEFILE FALSE)

# SET VARIABLES
set(CMAKE_CXX_COMPILER /usr/bin/g++)
set(Arcane_ROOT $ENV{HOME}/arcane/install)
set(N_WS_PATH $ENV{HOME}/workspaces/NabLab/plugins/fr.cea.nabla.ui/examples)
set(CARTESIANMESH2DARCANE_PATH ${N_WS_PATH}/.nablab/mesh/cartesianmesh2darcane)

if (NOT DEFINED Arcane_ROOT)
	message(FATAL_ERROR "Arcane_ROOT variable must be set")
endif()

project(Glace2d LANGUAGES C CXX)

find_package(Arcane REQUIRED)

# SUB_DIRECTORIES
if(NOT TARGET nablalib)
	add_subdirectory(${N_WS_PATH}/.nablab/nablalib ${CMAKE_BINARY_DIR}/nablalib)
endif()
if(NOT TARGET cartesianmesh2darcane)
	add_subdirectory(${CARTESIANMESH2DARCANE_PATH} ${CMAKE_BINARY_DIR}/cartesianmesh2darcane)
endif()

add_executable(glace2d Glace2dModule.cc Glace2d_axl.h main.cc)

arcane_generate_axl(Glace2d)
configure_file(Glace2d.config ${CMAKE_CURRENT_BINARY_DIR} @ONLY)
#arcane_add_arcane_libraries_to_target(glace2d)
arcane_accelerator_enable()
arcane_accelerator_add_source_files(Glace2dModule.cc)
arcane_accelerator_add_to_target(glace2d)
target_link_libraries(glace2d PRIVATE arcane_full nablalib cartesianmesh2darcane)
target_include_directories(glace2d PUBLIC . ${CMAKE_CURRENT_BINARY_DIR})

# OPTIONAL USER OPTIONS IN Project.cmake
if (EXISTS ${CMAKE_CURRENT_SOURCE_DIR}/Project.cmake)
	include(${CMAKE_CURRENT_SOURCE_DIR}/Project.cmake)
endif()
