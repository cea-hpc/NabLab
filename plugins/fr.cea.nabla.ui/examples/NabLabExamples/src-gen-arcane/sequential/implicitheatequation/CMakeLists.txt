### DO NOT EDIT THIS FILE - it is machine generated ###

cmake_minimum_required(VERSION 3.10)
cmake_policy(SET CMP0074 NEW)
set(CMAKE_VERBOSE_MAKEFILE FALSE)

# SET VARIABLES
set(CMAKE_CXX_COMPILER /usr/bin/g++)
set(Arcane_ROOT $ENV{HOME}/arcane/install)
set(N_WS_PATH $ENV{HOME}/workspaces/NabLab/plugins/fr.cea.nabla.ui/examples)
set(LINEARALGEBRASTL_PATH ${N_WS_PATH}/.nablab/linearalgebra/linearalgebrastl)
set(CARTESIANMESH2DARCANE_PATH ${N_WS_PATH}/.nablab/mesh/cartesianmesh2darcane)

if (NOT DEFINED Arcane_ROOT)
	message(FATAL_ERROR "Arcane_ROOT variable must be set")
endif()

project(ImplicitHeatEquation LANGUAGES C CXX)

find_package(Arcane REQUIRED)

# SUB_DIRECTORIES
if(NOT TARGET nablalib)
	add_subdirectory(${N_WS_PATH}/.nablab/nablalib ${CMAKE_BINARY_DIR}/nablalib)
endif()
if(NOT TARGET linearalgebrastl)
	add_subdirectory(${LINEARALGEBRASTL_PATH} ${CMAKE_BINARY_DIR}/linearalgebrastl)
endif()
if(NOT TARGET cartesianmesh2darcane)
	add_subdirectory(${CARTESIANMESH2DARCANE_PATH} ${CMAKE_BINARY_DIR}/cartesianmesh2darcane)
endif()

add_executable(implicitheatequation ImplicitHeatEquationModule.cc ImplicitHeatEquation_axl.h main.cc)

arcane_generate_axl(ImplicitHeatEquation)
configure_file(ImplicitHeatEquation.config ${CMAKE_CURRENT_BINARY_DIR} @ONLY)
#arcane_add_arcane_libraries_to_target(implicitheatequation)
target_link_libraries(implicitheatequation PRIVATE arcane_full nablalib linearalgebrastl cartesianmesh2darcane)
target_include_directories(implicitheatequation PUBLIC . ${CMAKE_CURRENT_BINARY_DIR})

# OPTIONAL USER OPTIONS IN Project.cmake
if (EXISTS ${CMAKE_CURRENT_SOURCE_DIR}/Project.cmake)
	include(${CMAKE_CURRENT_SOURCE_DIR}/Project.cmake)
endif()
